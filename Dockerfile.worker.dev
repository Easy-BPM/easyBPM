FROM eclipse-temurin:17-jdk

# Development Dockerfile for Worker: uses a locally-built jar from worker/build/libs
# This speeds up iterative development. Remove or ignore for production.
WORKDIR /app
COPY worker/build/libs/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java","-jar","/app/app.jar"]
