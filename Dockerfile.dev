FROM eclipse-temurin:17-jdk

# Development Dockerfile: uses a locally-built jar from build/libs
# This speeds up iterative development. Remove or ignore for production.
WORKDIR /app
COPY build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
